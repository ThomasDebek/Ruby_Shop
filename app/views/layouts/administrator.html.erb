<!DOCTYPE html>
<html>
<head>
  <title>RubyLoversShopTomasz</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
<%= render 'administrator/navigation' %>
<%= render 'flash_messages' %>
<div class="container">
  <div class="row">
    <div class="col-lg-9">
      <%= yield %>
    </div>

    <div class="col-lg-3">
      <h4 class="my-4">Categories</h4>
      <div class="list-group">
        <% Category.all.each do |category| %>
          <%= link_to category.name, administrator_root_path(category: category.id), class: "list-group-item" %>
        <% end %>
      </div>

      <h4 class="my-4">Brands</h4>
      <div class="list-group">
        <% Brand.all.each do |brand| %>
          <%= link_to brand.name, administrator_root_path(brand: brand.id), class: "list-group-item" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
</body>
</html>
